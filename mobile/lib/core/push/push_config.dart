/// Set to `true` once Firebase has been configured for the mobile app —
/// i.e. after running `flutterfire configure` (which generates
/// `lib/firebase_options.dart`) and adding `google-services.json`.
///
/// Until then every push code path no-ops so the app builds and runs normally
/// without a Firebase project.
const bool kFirebaseConfigured = true;
